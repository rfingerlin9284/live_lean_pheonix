version: '3.7'
services:
  rick_agent:
    build: .
    container_name: rick_agent
    restart: unless-stopped
    volumes:
      - ./narration.jsonl:/app/narration.jsonl
      - ./logs:/app/logs
    environment:
      - ENV=production
    command: bash start_with_integrity.sh
