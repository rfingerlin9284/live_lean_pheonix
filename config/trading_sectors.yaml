# Trading sectors configuration
# Balanced Profile defaults are applied as the base safety knobs
oanda:
  enabled: true
  mode: practice
  profile:
    MIN_EXPECTED_PNL_USD: 35.0
    MIN_NOTIONAL_USD: 10000
    MAX_MARGIN_UTILIZATION_PCT: 0.2

coinbase:
  enabled: true
  mode: canary
  profile:
    MIN_EXPECTED_PNL_USD: 35.0
    MIN_NOTIONAL_USD: 10000
    MAX_MARGIN_UTILIZATION_PCT: 0.25

ibkr:
  enabled: true
  mode: paper
  profile:
    MIN_EXPECTED_PNL_USD: 35.0
    MIN_NOTIONAL_USD: 10000
    MAX_MARGIN_UTILIZATION_PCT: 0.15
